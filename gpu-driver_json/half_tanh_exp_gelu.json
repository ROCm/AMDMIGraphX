{
  "compile_op": {
    "inputs": [
      {},
      {}
    ],
    "lambda": "[](auto x)\n{\n    using x_type = decltype(x);\n    x_type one = 1.;\n    x_type fit_const = 2.;\n    x_type sqrt2rpi = 3.;\n    auto x_b = x * sqrt2rpi;\n    auto u = x_b * fit_const * x * x + x_b;\n    auto emu = exp(-u - u);\n    auto cdf = one / (one + emu);\n    return x * cdf;\n}",
    "name": "pointwise"
  },
  "settings": {
    "iterations": 1000,
    "lens": [
      10,
      384,
      3072
    ],
    "type": "half_type"
  }
}
