{
  "compile_op": {
    "inputs": [
      {},
      {}
    ],
    "lambda": "[](auto x)\n{\n    using x_type = decltype(x);\n    x_type one = 1.;\n    x_type fit_const = 2.;\n    x_type three = 3.;\n    x_type sqrt2rpi = 4.;\n    x_type r2 = 0.5;\n    return r2 * x * (one + tanh(sqrt2rpi * (x + fit_const * pow(x, three))));\n}",
    "name": "pointwise"
  },
  "settings": {
    "iterations": 1000,
    "lens": [
      10,
      384,
      3072
    ],
    "type": "float"
  }
}
