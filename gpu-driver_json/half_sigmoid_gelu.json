{
  "compile_op": {
    "inputs": [
      {},
      {}
    ],
    "lambda": "[](auto x)\n{\n    using x_type = decltype(x);\n    x_type one = 1.;\n    x_type fit_const = 2.;\n    return x / (one + exp(-fit_const * x));\n}",
    "name": "pointwise"
  },
  "settings": {
    "iterations": 1000,
    "lens": [
      10,
      384,
      3072
    ],
    "type": "half_type"
  }
}
