# erf GELU
{
    settings: {
        iterations: 1000,
        lens: [10, 384, 3072],
#        strides: [384, 1],
        type: "float"
    },
    compile_op: {
        name: "pointwise",
        lambda: "[](auto one, auto r2, auto sqrt2, auto x) { return ( x * r2 * (one + erf(x / sqrt2)) );}",
        inputs: [{}, {}, {}, {}, {}]
    }
}
