# fp16 erf GELU
{
    settings: {
        iterations: 1000,
        lens: [10, 384, 3072],
#        strides: [1179648, 384, 1],
        type: "half_type"
    },
    compile_op: {
        name: "pointwise",
        lambda: "[](auto one, auto r2, auto sqrt2, auto x) { return ( x * r2 * (one + convert<half>(erf(x / sqrt2))) );}",
        inputs: [{}, {}, {}, {}, {}]
    }
}
